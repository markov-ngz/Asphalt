apiVersion: v1
kind: Service
metadata:
  name: erpcompany-db
  namespace: asphalt
spec:
  type: ClusterIP
  selector:
    app: erp-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432